PORT=3000

# For later phases
MONGODB_URI=mongodb://127.0.0.1:27017/frontdesk

# JWT secret will be used when auth is implemented
JWT_SECRET=change-me

# SMTP settings (only needed when we wire real email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM="FrontDesk <no-reply@frontdesk.local>"
